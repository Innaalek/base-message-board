export default function MessageBoard() {
  return (
    <div style={{padding: "20px", fontFamily: "Arial"}}>
      <h1>Base Message Board</h1>
      <button onClick={() => alert("Button works!")}>Publish</button>
    </div>
  );
}
